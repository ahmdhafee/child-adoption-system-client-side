# Prevent direct access to uploads directory

Options -Indexes

# Deny access to all files in uploads directory

<FilesMatch "\.(jpg|jpeg|png|pdf|txt)$">
Order Deny,Allow
Deny from all
</FilesMatch>

# Allow access only from PHP scripts

<FilesMatch "^download\.php$">
Order Allow,Deny
Allow from all
</FilesMatch>
